{% extends 'layout/app.html.twig' %}

{% block title %}Aucun dépôt{% endblock %}

{% block page_header %}
    <div class="mb-4">
        <h4 class="mb-1">Dépôt non configuré</h4>
        <p class="text-muted mb-0">Ajoutez un dépôt GitHub pour démarrer la synchronisation.</p>
    </div>
{% endblock %}

{% block page_content %}
    <div class="card">
        <div class="card-body text-center p-5">
            <i class="bx bx-folder-open text-primary fs-1 d-block mb-3"></i>
            <h5 class="mb-2">Aucun dépôt suivi</h5>
            <p class="text-muted mb-4">Demandez à un administrateur de configurer un dépôt pour pouvoir visualiser l'arborescence.</p>
            {% if is_granted('ROLE_ADMIN') %}
                <a class="btn btn-primary" href="{{ path('repository_index') }}">
                    <i class="bx bx-slider-alt me-2"></i>
                    <span>Configurer un dépôt</span>
                </a>
            {% endif %}
        </div>
    </div>
{% endblock %}
